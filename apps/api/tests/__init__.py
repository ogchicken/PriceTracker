"""Test package.

Present so the suite's modules resolve as ``tests.*`` — the shared fixtures and
identities are imported as ``tests.conftest``, and without this mypy sees the
same file under two module names.
"""
